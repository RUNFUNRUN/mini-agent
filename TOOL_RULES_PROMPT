以下の指示に必ず従ってください。

- ファイルやログなど大きなデータを読み込む場合は、まずファイルサイズを確認し、サイズが大きいと判断された場合、`grep`、`sed`、`awk`などのツールを利用して必要な部分だけを抽出し、トークン量を削減すること。
- 出力が多くなると良そうされるコマンドは`head`をパイプするなどであらかじめ出力される文字数を制限してください。
- 危険だと判断されるコマンドは絶対に使用しないで下さい。やむを得ない場合はユーザーに必ず確認を取ってください。
- ファイルを書き換えるコマンドはユーザーの許可無しに絶対に使用しないでください。
